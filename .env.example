SMTP_HOST=smtp.gmail.com
SMTP_PORT=465
SMTP_SECURE=true
SMTP_USER=izbooking1@gmail.com
SMTP_PASS=PUT_YOUR_APP_PASSWORD_HERE
FROM_NAME="IZ Booking"
FROM_EMAIL=izbooking1@gmail.com
PORT=4000


<!doctype html>
<html lang="ru">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>IZ Booking v7</title>
  </head>
  <body>
    <div id="root"></div>
    <script type="module" src="/src/main.jsx"></script>
  </body>
</html>

{
  "name": "iz-booking-v7-6",
  "private": true,
  "version": "8.2.0",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview",
    "server": "node server/index.js",
    "dev:all": "concurrently -k -n SERVER,CLIENT -c blue,magenta \"npm run server\" \"npm run dev\"",
    "vercel-build": "vite build"
  },
  "dependencies": {
    "react": "^18.3.1",
    "react-dom": "^18.3.1",
    "date-fns": "^3.6.0",
    "express": "^4.19.2",
    "cors": "^2.8.5",
    "nodemailer": "^6.9.13",
    "dotenv": "^16.4.5"
  },
  "devDependencies": {
    "@vitejs/plugin-react": "^4.3.1",
    "vite": "^5.4.0",
    "concurrently": "^8.2.2"
  }
}

# IZ Booking v7 (Vite + React)

- Механизм подтверждения: новая запись = `pending`, админ может `Approve` → `approved` или `Reject` → `canceled_admin`
- «Мои записи»: кнопка «Обновить» без перезагрузки страницы, фильтры, статусы
- i18n: RU (по умолчанию) / LT / EN, переключатель 🇱🇹 🇷🇺 🇬🇧 в шапке, переводы без перезагрузки
- Админка: сортировка, поиск, фильтры по статусу (pending/approved/canceled_*), экспорт CSV
- Календарь: слоты блокируются, если уже есть pending/approved запись на время
- Тёмная тема IZ HAIR TREND, логотип в шапке и футере, рабочие часы 04:00–20:00

Запуск:
```bash
npm i
npm run dev
```

IZ Booking v8.2.0
- Forgot Password shows temp password on screen (+ optional email)
- Profile edit + password change
- Admin-only reset log (Irina/Vladislav)
- Calendar preserved


import { defineConfig } from 'vite'
import react from '@vitejs/plugin-react'
export default defineConfig({ plugins: [react()], publicDir: 'public' })
